# 2026-03-20

## FACEMAXX progress saved

FACEMAXX was recovered, continued, and heavily advanced in the active workspace.

Project path:
- `/Users/moey/.openclaw/workspace/facemaxx-mobile`

Major work completed today:
- recovered FACEMAXX from workspace continuity files instead of restarting
- verified app builds with TypeScript and Expo web export
- completed Phase 4 improvement engine
- completed Phase 5 retention systems
- completed Phase 6 viral systems
- completed Phase 7 monetization framing
- added refinement pass:
  - timeline graph
  - best-version card
  - stronger optimization/potential positioning copy
  - upgraded share caption variations
  - upgraded battle mode toward real two-face compare
- upgraded score engine from old seed/hash-style mock behavior to image-derived local analysis using uploaded image metadata + byte-signal sampling
- upgraded battle mode to support a real second uploaded image and second analysis pass
- added proof/review checklist at `facemaxx-mobile/REVIEW_CHECKLIST.md`

Current honest status:
- FACEMAXX is prototype-complete / review-ready
- not yet production-grade
- best next step is proof capture + final visual review
- do NOT rush into real monetization plumbing before validating the current product loop

Key files for recovery:
- `HANDOFF.md`
- `NOW.md`
- `PROJECTS.md`
- `facemaxx-mobile/REVIEW_CHECKLIST.md`

Important recent commits:
- `a8f743b` — verify phase 3 and implement phase 4 improvement engine
- `47eab96` — implement phase 5 retention systems
- `b001ff9` — implement phase 6 viral systems
- `5b7d0a9` — implement phase 7 monetization
- `8f5e6b5` — refinement pass for retention, positioning, battle, and share
- `668cd2e` — tighten product review copy and label alignment
- `c61208a` — polish spacing and layout consistency
- `790d7e1` — add image-derived analysis and real second-image battle flow
- `4493e95` — add proof-capture and review checklist

If a new session starts, resume from the ML/calibration roadmap files before making backend decisions:
- `facemaxx-mobile/FACEMAXX_V2_ROADMAP.md`
- `facemaxx-mobile/measurement_schema.json`
- `facemaxx-mobile/dataset_schema.md`
- `facemaxx-mobile/MODEL_PLAN.md`
- `facemaxx-mobile/TRAINING_BACKLOG.md`

The next serious step is not more UI work — it is measurement logging, dataset design, and v1 model planning.

Update: measurement logging groundwork has begun in the app itself. Scan records now carry structured measurement/confidence/rejection data, and the workspace now includes a rating rubric plus initial training scaffold docs.

Further update: FACEMAXX scans now also create local training-style dataset export records, so measurement outputs can start feeding the future labeled dataset/model pipeline.

Newest update: dataset samples now also write out as file-based JSON exports through Expo file system, which is the first real bridge from app-side scanning into reusable training artifacts.

Newest newest update: `facemaxx-mobile/training/` now has a working labeling scaffold with raw export folders, annotation schema/template, merge rules, status docs, and a manifest builder script (`build_manifest.py`).

Latest update: the labeling pipeline now also has an annotation merge script (`merge_annotations.py`) plus usage docs, so raw exports + rater files can be aggregated into merged training labels automatically.

Latest architecture decision: Moey chose the fuller local analysis stack immediately — OpenCV + InsightFace + MediaPipe + FACEMAXX calibration. Workspace scaffolding now exists at `facemaxx-mobile/analysis-backend/`.

Latest backend update: the Python environment installed successfully, the full dependency stack resolved, and the backend health endpoint is live at `http://127.0.0.1:8089/health`.

Analyze test update: `/analyze` was tested with a real face test image. OpenCV preprocessing worked, InsightFace detection worked, and the calibration layer returned a structured FACEMAXX payload. MediaPipe landmark extraction was initially blocked because the installed runtime exposes `mediapipe.tasks` rather than the older `solutions.face_mesh` API.

Resolution: the landmarks module was adapted to the MediaPipe Tasks API, and `/analyze` now succeeds end-to-end with all three layers working: OpenCV preprocessing, InsightFace detection, and MediaPipe landmarks.

Latest app integration update: FACEMAXX now prefers the local backend at `http://127.0.0.1:8089/analyze` for primary scans and battle-image analysis, while keeping the existing heuristic scan path as fallback.

Latest mapping update: backend quality/detection/landmark signals now feed more directly into breakdown explanation copy and stored measurement quality fields in the app.

Latest measurement update: backend landmarks/calibration now emit richer ratio/symmetry-style measurements, and the app now threads more of those into stored measurements, identity copy, battle explanation, and recommendation detail.

Latest UI update: those backend measurements are now also visible in the app through a backend measurement panel and a geometry readout section.

Latest logic update: archetype and recommendation behavior are now pushed further toward measurement-driven mapping using jaw ratio, face ratio, symmetry, and backend quality/confidence signals.

Memory flush update:
- FACEMAXX local analysis backend is now materially real, not just planned. Python backend scaffold exists under `facemaxx-mobile/analysis-backend/` with OpenCV preprocessing, InsightFace detection/alignment scaffold, MediaPipe landmark scaffold, FastAPI server, and calibration layer.
- The Python environment and dependencies installed successfully, and backend health check passed at `http://127.0.0.1:8089/health`.
- `/analyze` was tested against a real face test image. Verified working: OpenCV preprocessing, InsightFace face detection, and FACEMAXX calibration payload generation.
- Current blocker: MediaPipe landmark extraction is not yet working in this runtime because the installed package exposes `mediapipe.tasks` but not the older `mediapipe.solutions.face_mesh` API expected by the initial scaffold.
- Important latest commits after the previous memory save include:
  - `7066aa1` — add incoming photo intake structure
  - `b1a2678` — expand participant intake templates
  - `7fb1b7d` — scaffold full local analysis stack plan
  - `7c8ce54` — begin Python analysis backend scaffold
  - `b4b8479` — install and boot local analysis backend
  - `c72ac96` — ignore Python cache files and venv
  - `3edacb3` — verify analyze endpoint and note MediaPipe blocker
- Current recommended next step: adapt the landmark module to use the installed MediaPipe runtime/API, then reconnect/test `/analyze` so landmarks feed the FACEMAXX calibration layer properly.


End-of-context save:
- FACEMAXX has moved far beyond the original prototype. The app now uses a real local analysis backend path by default for scans/battle analysis, with fallback to the older in-app heuristic path if the backend is unavailable.
- Local backend stack chosen and working: OpenCV + InsightFace + MediaPipe + FACEMAXX calibration.
- Backend location: `facemaxx-mobile/analysis-backend/`
- Backend health endpoint: `http://127.0.0.1:8089/health`
- Web app local preview: `http://127.0.0.1:19008/`
- Proven backend status:
  - OpenCV preprocessing works
  - InsightFace detection works
  - MediaPipe landmarks work via Tasks API
  - `/analyze` returns a structured FACEMAXX payload end-to-end
- App integration status:
  - app now prefers backend analysis for main scans and battle image analysis
  - richer backend measurements now feed stored measurement data, explanation copy, visible UI panels, battle reasoning, and recommendation detail
  - backend measurement panel + geometry readout are visible in UI
  - archetype/recommendation behavior is now more measurement-driven
- Training/data pipeline status:
  - structured measurement vectors exist in app scan records
  - confidence/rejection logic exists
  - scans create local dataset export records
  - scans also write file-based JSON sample exports
  - training scaffold exists under `facemaxx-mobile/training/`
  - labeling workflow exists
  - manifest builder exists
  - annotation merge workflow exists
  - first 25-sample batch packet exists
  - incoming participant folder structure + Desktop shortcut exist
- Easy-access training folder symlink: `~/Desktop/FACEMAXX-training`
- Current important training/backend docs to read first in a new session:
  - `facemaxx-mobile/FULL_STACK_INTEGRATION_PLAN.md`
  - `facemaxx-mobile/FACEMAXX_V2_ROADMAP.md`
  - `facemaxx-mobile/analysis-backend/README.md`
  - `facemaxx-mobile/analysis-backend/USAGE.md`
  - `facemaxx-mobile/training/STATUS.md`
  - `facemaxx-mobile/training/FIRST_BATCH_PLAN.md`
  - `facemaxx-mobile/REVIEW_CHECKLIST.md`
- Best next steps after context reset:
  1. Continue visual review/polish of the live app now that backend measurements are visible
  2. Tighten any weak copy/labels revealed in visual review (especially upload/result/developer-ish labels)
  3. Continue backend/app refinement so more measurement fields influence battle reasoning and recommendations
  4. In parallel, start collecting the first real consented participant batch and place exports into `training/raw-exports/`
- Most important recent commits near the current state:
  - `f7b18e9` — fix MediaPipe integration and pass analyze test
  - `3b70047` — connect app scans to local analysis backend
  - `91ff585` — deepen backend response mapping in app
  - `28bd2a3` — enrich backend measurements and app mapping
  - `ed3edc3` — surface backend measurements in UI
  - `1ddaa56` — make archetype and recommendations more measurement-driven
