# 2026-03-20

## FACEMAXX progress saved

FACEMAXX was recovered, continued, and heavily advanced in the active workspace.

Project path:
- `/Users/moey/.openclaw/workspace/facemaxx-mobile`

Major work completed today:
- recovered FACEMAXX from workspace continuity files instead of restarting
- verified app builds with TypeScript and Expo web export
- completed Phase 4 improvement engine
- completed Phase 5 retention systems
- completed Phase 6 viral systems
- completed Phase 7 monetization framing
- added refinement pass:
  - timeline graph
  - best-version card
  - stronger optimization/potential positioning copy
  - upgraded share caption variations
  - upgraded battle mode toward real two-face compare
- upgraded score engine from old seed/hash-style mock behavior to image-derived local analysis using uploaded image metadata + byte-signal sampling
- upgraded battle mode to support a real second uploaded image and second analysis pass
- added proof/review checklist at `facemaxx-mobile/REVIEW_CHECKLIST.md`

Current honest status:
- FACEMAXX is prototype-complete / review-ready
- not yet production-grade
- best next step is proof capture + final visual review
- do NOT rush into real monetization plumbing before validating the current product loop

Key files for recovery:
- `HANDOFF.md`
- `NOW.md`
- `PROJECTS.md`
- `facemaxx-mobile/REVIEW_CHECKLIST.md`

Important recent commits:
- `a8f743b` — verify phase 3 and implement phase 4 improvement engine
- `47eab96` — implement phase 5 retention systems
- `b001ff9` — implement phase 6 viral systems
- `5b7d0a9` — implement phase 7 monetization
- `8f5e6b5` — refinement pass for retention, positioning, battle, and share
- `668cd2e` — tighten product review copy and label alignment
- `c61208a` — polish spacing and layout consistency
- `790d7e1` — add image-derived analysis and real second-image battle flow
- `4493e95` — add proof-capture and review checklist

If a new session starts, resume from the ML/calibration roadmap files before making backend decisions:
- `facemaxx-mobile/FACEMAXX_V2_ROADMAP.md`
- `facemaxx-mobile/measurement_schema.json`
- `facemaxx-mobile/dataset_schema.md`
- `facemaxx-mobile/MODEL_PLAN.md`
- `facemaxx-mobile/TRAINING_BACKLOG.md`

The next serious step is not more UI work — it is measurement logging, dataset design, and v1 model planning.

Update: measurement logging groundwork has begun in the app itself. Scan records now carry structured measurement/confidence/rejection data, and the workspace now includes a rating rubric plus initial training scaffold docs.

Further update: FACEMAXX scans now also create local training-style dataset export records, so measurement outputs can start feeding the future labeled dataset/model pipeline.

Newest update: dataset samples now also write out as file-based JSON exports through Expo file system, which is the first real bridge from app-side scanning into reusable training artifacts.

Newest newest update: `facemaxx-mobile/training/` now has a working labeling scaffold with raw export folders, annotation schema/template, merge rules, status docs, and a manifest builder script (`build_manifest.py`).

Latest update: the labeling pipeline now also has an annotation merge script (`merge_annotations.py`) plus usage docs, so raw exports + rater files can be aggregated into merged training labels automatically.

Latest architecture decision: Moey chose the fuller local analysis stack immediately — OpenCV + InsightFace + MediaPipe + FACEMAXX calibration. Workspace scaffolding now exists at `facemaxx-mobile/analysis-backend/`.

Latest backend update: the Python environment installed successfully, the full dependency stack resolved, and the backend health endpoint is live at `http://127.0.0.1:8089/health`.

Analyze test update: `/analyze` was tested with a real face test image. OpenCV preprocessing worked, InsightFace detection worked, and the calibration layer returned a structured FACEMAXX payload. MediaPipe landmark extraction is the current blocker because the installed runtime exposes `mediapipe.tasks` but not the older `solutions.face_mesh` API.
