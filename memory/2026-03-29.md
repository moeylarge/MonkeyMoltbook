## 2026-03-29

- MonkeyMoltbook / MOLT-LIVE homepage cleanup pass was executed directly in `MonkeyMoltbook/apps/web/src/App.jsx` with supporting CSS updates in `MonkeyMoltbook/apps/web/src/styles.css`; the homepage was materially simplified by removing homepage proof/monetization/discovery/waitlist/FAQ clutter and reducing it toward a smaller hero + compact discovery preview + featured live section + final CTA structure.
- The homepage removal review was preserved first in `MonkeyMoltbook/homepage-review/2026-03-29-homepage-removal-candidates.md` so deleted homepage ideas can be selectively restored later if needed.
- Workspace commits created for this homepage pass:
  - `ee63db4` — `Add MOLT-LIVE homepage removal review notes`
  - `2a2fe5c` — `Simplify MOLT-LIVE homepage structure`
- MonkeyMoltbook deploy path was verified as Vercel from the project root using `.vercel/project.json` and `vercel.json`; production deployment still aliases to `https://molt-live.com`.
- The simplified homepage pass was deployed live to `https://molt-live.com` on 2026-03-29.
- Immediate follow-up homepage copy tweaks were also pushed live on 2026-03-29:
  - the highlighted status pill text was changed from `Live now` to `Chat Direct`
  - the hero headline was changed to: `Open a live AI feed and jump into camera-ready sessions fast, or explore Direct Chat Messaging.`
- Current operating preference explicitly stated by John in chat: keep replies minimal, direct, and with minimal paragraphs for this interaction mode.
- MonkeyMoltbook / MOLT-LIVE product direction clarified: a major product differentiator should be a visible trust/risk protocol that scores communities, posts, memberships, and accounts for scam/spam/hacker/malware-risk style signals, surfaces that score on the right side of cards/pages, and helps users know what to stay away from.
- A concrete project spec was written at `MonkeyMoltbook/TRUST_SCORE_V1_SPEC.md` defining V1 trust/risk scoring for communities, posts, accounts, and memberships, including score bands, explainability, UI placement, storage tables, signal families, and phased implementation order.
- Initial trust-layer implementation was started: `MonkeyMoltbook/supabase/schema.sql` now includes `entity_risk_scores`, `entity_risk_events`, and `risky_domains`; `apps/server/src/lib/trust-score.js` was added with heuristic V1 scoring; server endpoints now attach trust data to authors/communities/submolts and persist basic scores during collector/expanded ingest; the web UI now renders visible right-side trust badges on agent/community/group surfaces.
- Workspace commits for the trust-layer work on 2026-03-29:
  - `5e10fd4` — `Add MOLT-LIVE trust score v1 spec`
  - `4983075` — `Wire MOLT-LIVE trust score scaffold and badges`
- Near-term execution order for MOLT-LIVE was reinforced: let the rolling collector keep running to accumulate more data, then audit coverage/search quality, then improve search ordering/relevance; trust scoring has now been scaffolded in parallel so later ranking/demotion can use that layer.
- Production check against live Supabase confirmed `entity_risk_scores` does not exist yet in schema cache, so the trust-table SQL still needs to be applied before persisted trust scoring will work live.
- Trust heuristic tuning was adjusted against real live corpus examples to reduce false positives: terms like hackathon/HACKAI/free-guidance/security-research/compliance now dampen score, claimed/high-karma accounts are treated more conservatively, and single weak language hits without supporting link/behavior evidence are less likely to produce elevated risk labels.
- The real bottleneck for the trust layer turned out to be data depth, not just scoring weights: rolling collection alone kept hitting a shallow/repeated slice, while expanded ingest (`/moltbook/ingest/expanded`) materially deepened community coverage.
- After expanded ingest on 2026-03-29, `mbc20` jumped from a thin stored community row to `post_count` 154 with richer sample titles and its trust score moved to `50 / High Risk`, confirming that richer ingestion is required for reliable community-level risk judgments.
- Community storage/upsert logic was patched to accumulate stronger community coverage across runs (preserving higher `post_count` and merged `sampleTitles`) instead of overwriting rows with only the latest thin batch.
- Even after improving stored community truth, group search for suspicious queries like `mint` / `hackai` remained sparse because that search path still relied too much on community name/description matching rather than deeper post-content/community-evidence retrieval; this became the next search-layer fix.
- The trust/search pipeline was materially strengthened on 2026-03-29: group search now merges stored community rows with post-backed community evidence, post storage derives `submolt_name` from payload when missing, and suspicious community queries like `hackai` now surface `mbc20` correctly with severe/high-risk trust labels.
- Key debugging finding: many mint/HACKAI posts are genuinely posted into Moltbook `general`, so `mint` is an inherently mixed query; ranking fixes alone were insufficient without adding a specialized mint-intent search path.
- A specialized `mint` community search path was added so inscription/token communities are prioritized using stronger evidence patterns (`mbc20`, `mbc-20`, HACKAI/BOT/WANG mint-style contexts, JSON mint payloads, `mbc20.xyz`), and broad `general` results are suppressed when specialized evidence exists.
- By the end of the session, `mint` group search was materially improved: `mbc20` ranked first, `mbc-20` also surfaced with elevated risk, and the earlier failure mode where `general` dominated the top results was corrected.
- Remaining caveat at checkpoint: some search result shapes still show thinner trust state than the deeper `/community/:slug` page because search can hit a thinner stored row/evidence shape than the richer community detail state; broader audit should now proceed from this stronger base rather than more blind tuning.
