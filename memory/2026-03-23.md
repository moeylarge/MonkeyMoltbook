# 2026-03-23

## LooksMaxx continuity correction

Moey clarified the actual last launch-prep state for LooksMaxx so future sessions do not drift.

### Product decision
- LooksMaxx should launch as a **free product first** for either:
  - **2–4 weeks**, or
  - until roughly **1000 users**
- This free period is for learning/traction/feedback before pushing harder on monetization.

### App Store naming locked
- Primary app name: `LooksMaxx`
- Fallback 1: `LooksMaxx: Looks Rater`
- Fallback 2: `LooksMaxx: Looks Rating System`

### Subtitle locked
- `Looks rating, face analysis, beauty scan`

### Keywords locked
- `looksmax,looksmaxxing,face analysis,looks rating,glow up,attractiveness test,face scan,beauty score,archetype,selfie,beautymaxx,beautymax`

### Description locked
LooksMaxx gives you a sharper read on how your current presentation lands — and where your biggest gains are hiding.

Upload a photo to get:
- a looks read
- score and potential
- archetype interpretation
- category breakdowns
- improvement guidance
- compare / battle mode
- progress tracking

See what is helping your score, what is holding it back, and what changes are most likely to improve the result.

### App Store screenshot order locked
1. `How attractive are you, really?` → Home / first hook screen
2. `Get your score, archetype, and hidden upside` → Result / teaser screen
3. `See what’s making you more attractive — and what’s holding you back` → Full result / premium interpretation screen
4. `Break down the exact features shaping your result` → Breakdown screen
5. `Get the fastest path to a stronger read` → Plan / improvement roadmap screen
6. `Battle your face against another look` → Battle / compare mode
7. `Track your glow-up and improve over time` → History / progress / timeline screen

### Launch checklist state
Moey and the agent already created the Apple submission / launch checklist together.

Short version of the checklist:
- pay Apple developer fee
- confirm developer account is active
- open App Store Connect
- create the app
- confirm bundle ID matches project
- set app price to Free
- fill listing copy
- fill metadata/category/age rating/privacy/support/marketing URLs
- finalize app icon and screenshots
- run product QA on real device
- remove broken copy / unfinished language / misleading pricing
- build release version
- upload build to App Store Connect
- fill review info and permissions text
- submit for review

### Launch rule that was explicitly set
- **No new feature ideas**
- **No random redesigns**
- **Only fix real launch blockers**
- **Submit**

## UFC continuity correction

John clarified that the UFC project also included a website at:
- `https://www.ufcpickspro.com/`

John also stated:
- the site was deployed on Vercel
- password protection was added
- Moet has access to the Vercel side as well

Current recovery caveat:
- live-site verification confirms `https://www.ufcpickspro.com/` is on **Vercel** and currently protected by **HTTP Basic Auth**
- confirmed auth header: `www-authenticate: Basic realm="UFC Picks Pro", charset="UTF-8"`
- Vercel dashboard evidence from the user session confirms:
  - domain `ufcpickspro.com` is active
  - registrar: third party
  - nameservers: third party
  - Vercel CDN: active
  - age shown: Mar 18
  - visible DNS record: root `A` record → `76.76.21.21` with `TTL 60`
  - Vercel DNS is not enabled from the shown screen
- the visible local workspace snapshot for `ufc-operator-web/` still does **not** currently preserve obvious Vercel config, password-protection code, or deployment metadata
- so this part of UFC continuity is only partially recoverable locally and may still require checking Vercel directly if UFC work resumes

## Worth promoting to durable memory
- LooksMaxx near-term launch strategy changed: free first for 2–4 weeks or ~1000 users.
- Apple submission checklist and App Store listing block are already defined.
- UFC project continuity includes a Vercel-deployed site at `ufcpickspro.com` with password protection, but current local files do not preserve the deploy details.

## Core continuity system reconciliation

Durable identity / continuity corrections were finalized today:
- agent name is **Moet**
- user name is **John**
- `IDENTITY.md` is the canonical identity file (not `.IDENTITY.md`)

Core continuity system is now standardized around:
- `SOUL.md`
- `IDENTITY.md`
- `USER.md`
- `PROJECTS.md`
- `NOW.md`
- `HANDOFF.md`
- `MEMORY.md`
- project-local `HANDOFF.md` / `STATUS.md`

Old `SESSION_HANDOFF.md` / `SESSION_STATE.md` references were removed from the core-doc system so future recovery should use the actual live file layout instead of a parallel naming scheme.

Startup/recovery truth to preserve:
- this workspace should not reset into stranger-mode when continuity files exist
- future recovery should treat LooksMaxx as recoverable from structured files, not from vague transcript memory
- project-local `HANDOFF.md` / `STATUS.md` files now exist for LooksMaxx, UFC, and Social Clip OS and should be preferred for implementation truth

## UFC recovery / current-state save

UFC project is in a much better recovered state now.

### Repo / deploy recovery
- the real UFC site repo was recovered from:
  - `/Users/moey/.openclaw_old/workspace/ufc-operator-web`
- safe recovered copies now exist in the active workspace:
  - `/Users/moey/.openclaw/workspace/ufc-operator-web-recovered`
  - `/Users/moey/.openclaw/workspace/ufc-operator-web-restored-clean`
- live site:
  - `https://www.ufcpickspro.com/`
- hosting/deploy truth:
  - deployed on Vercel
  - protected by HTTP Basic Auth
  - confirmed auth realm: `UFC Picks Pro`
- Vercel/domain facts confirmed from dashboard + live checks:
  - domain `ufcpickspro.com` active
  - registrar: third party
  - nameservers: third party
  - Vercel CDN active
  - visible root A record → `76.76.21.21`
  - Vercel DNS not enabled from the shown screen

### Recovered app truth
- app is a real Next.js site with Vercel linkage
- Basic Auth is implemented in `src/middleware.ts`
- important historical handoff files in old workspace confirmed:
  - Vercel deploy usage
  - alias to `ufcpickspro.com`
  - Basic Auth intentional
  - external Postgres direction
  - Vercel Hobby cron/deploy constraints

### Saturday event ingestion / settlement work completed
- target event: `UFC Fight Night: Evloev vs. Murphy`
- this event had not been ingested into the visible analytics outputs before today
- completed-fights ingestion was run via:
  - `./.venv/bin/python src/train_model.py`
  in `/Users/moey/.openclaw_old/workspace/ufc-analytics`
- result:
  - `historical_fights` now contains **13** rows for the Evloev/Murphy card
  - winners populated in historical analytics data
- downstream rebuilds were run:
  - `src/generate_report.py`
  - `src/build_public_betting_feed.py`
  - `src/build_operator_snapshot.py`
  - `src/build_website_bundle.py`
- rebuilt analytics outputs were synced into the recovered web app `public/data/`

### Ledger settlement work completed
- live ledger had Saturday rows still pending before today
- Saturday bet rows were settled using recovered historical winners + latest market closing odds
- final Saturday ledger summary after settlement:
  - updated rows: **1520**
  - closing odds filled: **1520**
  - pending: **0**
  - wins: **304**
  - losses: **1216**
  - missing closing odds: **0**

### Live odds / freshness fixes completed
- one-time live odds refresh was run successfully from The Odds API
- successful refresh output showed:
  - fetched at `2026-03-23T19:14:06.246Z`
  - rows fetched: `35`
  - changed: `true`
  - deploy triggered successfully
- the 12-hour LaunchAgent still exists and was repaired to point at the recovered repo instead of the dead thin folder
- refresh cadence to remember:
  - `com.moey.ufc-operator-live-odds` = every **12 hours**
  - `com.moey.ufc-operator-live-odds-fast` = disabled

### Important code fixes made in recovered repo
- hardcoded old repo paths were fixed in:
  - `scripts/refresh-live-odds.mjs`
  - `scripts/fetch-live-odds.mjs`
- live odds storage mismatch was fixed:
  - refresh scripts wrote to SQLite
  - app runtime had been reading Postgres live-odds tables
  - app was updated to read the same SQLite live-odds store the refresh scripts write
- merged live-odds edge calculation was patched so homepage bet recording would not fail on null `edge_at_pick`

### Final verified UFC homepage state
Local verification now shows:
- header `Last refresh` is current
- homepage `Odds snapshot` is current
- homepage `Data freshness` is current
- Saturday fight data appears in the app
- performance/ledger rows are settled
- top header event may still show a future event like `UFC 327: Prochazka vs. Ulberg` because current layout logic uses the first best-approved operator spot, not the most recent completed event

### Useful recovery rule for next session
Before next Saturday's fights, resume from:
1. `ufc-operator-web/HANDOFF.md`
2. `/Users/moey/.openclaw/workspace/ufc-operator-web-recovered`
3. `/Users/moey/.openclaw_old/workspace/ufc-analytics`

Likely next UFC focus in a few days:
- improve site presentation/strategy before Saturday
- refresh odds again near the card
- adjust strategy/selection logic as needed

## LooksMaxx App Store launch push — release prep state

### Launch-direction confirmation
- John explicitly confirmed the shortest launch path for LooksMaxx is:
  1. final copy sweep in `App.tsx`
  2. build iOS release with EAS
  3. upload / submit in App Store Connect
- John reiterated the launch pricing rule:
  - users should be **free for 2–4 weeks**
  - app/store messaging should reflect that free launch window

### Release config + app changes completed
In `facemaxx-mobile/` the agent completed a concrete release-prep pass:
- `app.json` updated to release identity:
  - app name: `LooksMaxx`
  - slug: `looksmaxx`
  - iOS bundle identifier: `com.moey.looksmaxx`
  - Android package: `com.moey.looksmaxx`
  - iOS permission strings added for camera / photo library / save-to-library
- `eas.json` created for production build flow
- app launch access behavior was shifted toward **free launch access by default**
- major launch-copy sweep was done in `App.tsx` to better align with:
  - free launch window
  - Pro later / not active now
  - LooksMaxx naming instead of older LooksMaxxing naming

### Important repo commits made
Inside `facemaxx-mobile/.git` these commits were created:
- `2523a76` — `Prepare LooksMaxx free-launch release config`
- `4272049` — `Align LooksMaxx launch copy with free release`

### Build-system / auth checkpoint
- local config validation succeeded:
  - `npx expo config --json` worked
  - TypeScript compile was repaired and made to pass again after a stray trailing token issue at the end of `App.tsx`
- EAS CLI is not globally installed, but works via:
  - `npx eas-cli`
- attempted iOS build command:
  - `npx eas-cli build --platform ios --profile production`
- hard blocker reached:
  - CLI requires an **Expo/EAS account login** first
- user supplied intended Expo identity:
  - email: `moeylarge@gmail.com`
  - username: `moeylarge`
- login attempt with provided password failed with:
  - `Your username, email, or password was incorrect.`
- recovery state at compaction time:
  - user says they are logged into Expo on the website already
  - likely issue is website session vs CLI auth mismatch, or a different auth method (Google / magic link / etc.)
  - next question to resume from:
    - determine **which Expo login method** John used on expo.dev
    - then continue CLI auth accordingly before resuming the iOS EAS build

### Practical next step after recovery
Resume from:
1. ask / confirm how John logged into expo.dev (email+password vs Google vs GitHub vs magic link)
2. authenticate EAS CLI using the matching method
3. rerun:
   - `npx eas-cli build --platform ios --profile production`
4. continue to Apple/App Store Connect auth only after Expo auth succeeds

## LooksMaxx App Store submission completed
- The iOS build eventually succeeded after removing `expo-face-detector`, which had been causing native iOS Xcode build failures (`EXLogError` / `EXLogWarn`).
- Important build-fix commit:
  - `ec99812` — `Remove expo-face-detector to unblock iOS build`
- Public URLs created and used for App Store Connect:
  - `https://looksmaxx-site.vercel.app`
  - `https://looksmaxx-site.vercel.app/privacy`
  - `https://looksmaxx-site.vercel.app/support`
- Draft screenshot packs were generated locally for both iPhone and iPad App Store requirements.
- App Store Connect work completed in-session:
  - screenshots uploaded
  - metadata filled
  - app privacy completed
  - digital services act completed
  - build attached
  - submission sent
- Final state at end of session:
  - LooksMaxx status in App Store Connect = **Waiting for Review**
- Next-session rule:
  - do not redo launch-prep steps; only monitor Apple review result and respond if Apple requests changes.
